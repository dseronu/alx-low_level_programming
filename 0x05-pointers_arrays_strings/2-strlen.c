#include "main.h"
#include <stdio.h>

/**
 * int_strlen - function that returns the len of string
 * @*s: string
 * return: always 0
 */

int_strlen(char *s)
{
	int len;

	len = strlen(*s[6]="Hello");
	return (0);
}
