#include "main.h"

/**
 * _strlen - function that returns the len of string
 * @s: pointer input
 * return: i
 */

int _strlen(char *s)
{
	int len;

	for(len = 0; s[len] !='\0'; len++)
	{
	}
	
	return (i);
}
