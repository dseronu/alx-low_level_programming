#include "main.h"
#include <stdio.h>

/**
 * _strlen - function that returns the len of string
 * @*s: string
 * return: always 0
 */

int _strlen(char *s)
{
	int len;

	len = strlen(*s[6]="Hello");
	return (0);
}
